<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title><%= content_for(:title) || "Factura" %></title>
    <%= wicked_pdf_stylesheet_link_tag "application" %>
    <style>
      body { font-family: "Helvetica", sans-serif; font-size: 12px; color: #333; margin: 20px; }
      header { border-bottom: 1px solid #ccc; margin-bottom: 20px; padding-bottom: 10px; }
      header img { height: 50px; vertical-align: middle; margin-right: 10px; }
      header h1 { display: inline-block; font-size: 20px; font-weight: bold; margin: 0; }
      footer { border-top: 1px solid #ccc; margin-top: 30px; font-size: 10px; color: #666; text-align: center; padding-top: 10px; }
    </style>
  </head>
  <body>
    <header>
      <%= wicked_pdf_image_tag "icon.png", height: "50" %>
      <h1>RecordsOnRails</h1>
    </header>

    <%= yield %>

    <footer>
      Factura generada automáticamente el <%= l Time.current, format: :long %><br>
      Gracias por su compra.
    </footer>
  </body>
</html>